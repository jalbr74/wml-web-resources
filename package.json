{
    "name": "wml-web-resources",
    "version": "1.0.0",
    "main": "index.js",
    "scripts": {
        "clean": "npx rimraf dist",
        "build": "esbuild src/*.ts --bundle --minify --sourcemap --outdir=./dist --global-name=jda"
    },
    "devDependencies": {
        "@types/xrm": "^9.0.80",
        "esbuild": "^0.21.5"
    }
}
